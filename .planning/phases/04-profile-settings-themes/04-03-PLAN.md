---
phase: 04-profile-settings-themes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - screens/inbox.js
autonomous: true
requirements: [PROF-06]
must_haves:
  truths:
    - "User sees their own avatar initial in the top-right of the inbox topbar"
    - "Tapping that avatar navigates to #/settings"
    - "The existing + (new conversation) button still works and stays in the topbar"
  artifacts:
    - path: "screens/inbox.js"
      provides: "Inbox topbar with avatar button next to + button"
      contains: "inbox-avatar"
  key_links:
    - from: "screens/inbox.js avatar button click"
      to: "router.navigate('#/settings')"
      via: "click event listener"
      pattern: "router\\.navigate\\('#/settings'\\)"
---

<objective>
Implement PROF-06: add the user's avatar initial as a tappable button in the inbox topbar (top-right corner). Tapping it navigates to `#/settings`. This is the primary settings entry point — there is no gear icon (D-02).

This plan is independent of Plans 01 and 02 (touches only `screens/inbox.js`) — runs parallel in Wave 1. The avatar button uses `_strColor` and `_esc` (already imported in inbox.js) and the existing `.icon-btn` and `.avatar` CSS classes (existing in style.css, no Plan 01 dependency).

Purpose: Without this, there is no way for the user to reach `#/settings` from the inbox — the settings screen is currently orphaned in the route map (only reachable by manually editing the URL hash).
Output: Inbox topbar has order `[CIPHER title] | [+ new btn] [avatar btn]`. Tapping avatar → settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-profile-settings-themes/04-CONTEXT.md
@.planning/phases/04-profile-settings-themes/04-RESEARCH.md
@.planning/phases/04-profile-settings-themes/04-UI-SPEC.md
@screens/inbox.js
@lib/utils.js
@lib/router.js

<interfaces>
<!-- screens/inbox.js current topbar HTML (lines 24-29): -->
<!-- <div class="topbar"> -->
<!--   <div class="topbar-title">CIPHER</div> -->
<!--   <div class="topbar-actions"> -->
<!--     <button class="icon-btn" id="inbox-new" type="button" aria-label="New conversation">+</button> -->
<!--   </div> -->
<!-- </div> -->

<!-- _strColor signature (lib/utils.js): -->
<!-- export function _strColor(str: string): string  // returns 'hsl(h, 30%, 22%)' -->
<!-- export function _esc(s: any): string  // HTML-escape -->

<!-- router.navigate signature (lib/router.js): -->
<!-- export function navigate(hash: string): void -->
<!-- (sets window.location.hash) -->

<!-- store.get('profile') shape (already loaded by app.js boot): -->
<!-- { id, username, display_name, avatar_seed, theme?, created_at } -->
<!-- May be null briefly on first paint — guard accordingly. -->

<!-- Existing CSS classes used: -->
<!-- .topbar, .topbar-actions, .icon-btn (44x44 tap target), .avatar (existing pattern from .conv-row .avatar) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add avatar button to inbox topbar with click → #/settings navigation</name>
  <files>screens/inbox.js</files>
  <read_first>
    - screens/inbox.js (full file, especially lines 22-37 topbar HTML, lines 38-39 element grabs, lines 121-130 click wiring, lines 165-171 cleanup return)
    - lib/utils.js (confirm `_strColor` and `_esc` exports — both already imported at line 11)
  </read_first>
  <action>
    Edit `screens/inbox.js`. Three coordinated changes:

    (a) Compute the user's avatar seed BEFORE rendering the shell. After line 18 (the `if (!store.get('profile'))` guard) and BEFORE the `app.innerHTML = ` assignment (line 23), insert:

    ```js

      // Own-user avatar values for topbar button (PROF-06).
      const profile     = store.get('profile')
      const myUsername  = profile?.username || ''
      const myDisplay   = profile?.display_name || myUsername
      const myInitial   = (myDisplay[0] || myUsername[0] || '?').toUpperCase()
      const myAvatarBg  = _strColor(myUsername)
    ```

    (b) Replace the topbar block in the `app.innerHTML` template literal. Currently the topbar is:

    ```html
    <div class="topbar">
      <div class="topbar-title">CIPHER</div>
      <div class="topbar-actions">
        <button class="icon-btn" id="inbox-new" type="button" aria-label="New conversation">+</button>
      </div>
    </div>
    ```

    Replace with (the avatar button goes AFTER the + button so it sits at the rightmost edge per D-02 "top-right corner"):

    ```html
    <div class="topbar">
      <div class="topbar-title">CIPHER</div>
      <div class="topbar-actions">
        <button class="icon-btn" id="inbox-new" type="button" aria-label="New conversation">+</button>
        <button class="icon-btn avatar-btn" id="inbox-avatar" type="button" aria-label="Open settings">
          <div class="avatar" style="background:${myAvatarBg};width:32px;height:32px;font-size:14px;font-weight:500">${_esc(myInitial)}</div>
        </button>
      </div>
    </div>
    ```

    (c) Wire the click handler. After the existing line `const newBtn = app.querySelector('#inbox-new')` (currently line 39), add:

    ```js
      const avatarBtn = app.querySelector('#inbox-avatar')
    ```

    After the existing `function onNewClick() { router.navigate('#/new') }` (currently around line 126-128), add a new handler:

    ```js
      function onAvatarClick() {
        router.navigate('#/settings')
      }
    ```

    Wire the listener. After the line `newBtn.addEventListener('click', onNewClick)` (currently line 130), add:

    ```js
      avatarBtn.addEventListener('click', onAvatarClick)
    ```

    Update the cleanup return block (currently lines 166-171). After the line `newBtn.removeEventListener('click', onNewClick)`, add:

    ```js
        avatarBtn.removeEventListener('click', onAvatarClick)
    ```

    Do NOT modify any other code in this file (realtime subscriptions, conversation row rendering, message preview logic — all stay).
  </action>
  <verify>
    <automated>powershell -Command "$f='screens/inbox.js'; if ((Select-String -Path $f -Pattern 'id=\"inbox-avatar\"' -Quiet) -and (Select-String -Path $f -Pattern 'avatar-btn' -Quiet) -and (Select-String -Path $f -Pattern \"router.navigate\\('#/settings'\\)\" -Quiet) -and (Select-String -Path $f -Pattern 'aria-label=\"Open settings\"' -Quiet) -and (Select-String -Path $f -Pattern 'avatarBtn.addEventListener' -Quiet) -and (Select-String -Path $f -Pattern 'avatarBtn.removeEventListener' -Quiet) -and (Select-String -Path $f -Pattern 'id=\"inbox-new\"' -Quiet) -and (Select-String -Path $f -Pattern 'subscribeConversationMembers' -Quiet)) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `screens/inbox.js` contains exact substring `id="inbox-avatar"`
    - `screens/inbox.js` contains exact substring `aria-label="Open settings"`
    - `screens/inbox.js` contains exact substring `class="icon-btn avatar-btn"`
    - `screens/inbox.js` contains `router.navigate('#/settings')` (literal)
    - `screens/inbox.js` contains `avatarBtn.addEventListener('click', onAvatarClick)`
    - `screens/inbox.js` contains `avatarBtn.removeEventListener('click', onAvatarClick)` in cleanup
    - `screens/inbox.js` STILL contains `id="inbox-new"` (existing + button preserved)
    - `screens/inbox.js` STILL contains `subscribeConversationMembers` and `subscribeMessages` (realtime untouched)
    - `screens/inbox.js` STILL contains `_strColor` and `_esc` imports (line 11) — no new imports needed
    - The avatar button HTML contains inline style `width:32px;height:32px` (32px circle within 44px tap target)
    - The handler `onAvatarClick` calls `router.navigate('#/settings')` and nothing else
  </acceptance_criteria>
  <done>Open app, sign in, view inbox. Top-right of topbar shows two buttons: `+` then a colored avatar circle with user's initial. Tapping the avatar navigates to `#/settings`. Tapping `+` still navigates to `#/new`. No console errors. Cleanup runs on screen unmount (no leaked listeners).</done>
</task>

</tasks>

<verification>
- Manual: open app in browser, sign in, view inbox. Avatar button visible top-right with correct initial and color.
- Manual: tap avatar → URL changes to `#/settings`, settings screen renders.
- Manual: tap `+` button still works (URL → `#/new`).
- DevTools: confirm no console errors on inbox mount or unmount.
</verification>

<success_criteria>
- Task 1 acceptance criteria pass.
- PROF-06 truth ("avatar in topbar, taps to settings") observable in browser.
- No regressions: inbox list, realtime subscriptions, + button all still functional.
</success_criteria>

<output>
After completion, create `.planning/phases/04-profile-settings-themes/04-03-SUMMARY.md` documenting:
- Topbar HTML structure change (added second .icon-btn)
- New event handler pattern (onAvatarClick → router.navigate)
- Cleanup updated (removeEventListener added)
- Confirmation that existing realtime subscription logic is unchanged
</output>
